import React from "react";

const Color: React.FC = () => {
  return (
    <div className="fixed inset-0 z-40 p-1 w-full h-full bg-gradient-to-t to-transparent opacity-40 pointer-events-none from-bg-dark" />
  );
};

export default Color;
